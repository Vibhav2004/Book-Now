export let movievalue;

document.addEventListener("DOMContentLoaded", () => {
  const button = document.querySelector("#bookticket");

  if (!button) {
    console.error("Element with ID 'bookticket' not found.");
    return;
  }

  button.addEventListener("click", () => {
   
  });
});
